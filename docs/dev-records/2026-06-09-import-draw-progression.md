# KTTA-Platform 開発記録 — 2026-06-09

> 釧路卓球協会 大会運営アプリ（リポジトリ `AKIHIKO-S/kushiro` / name `ktta-platform`）。
> 本記録は「取込 → 抽選 → 当日進行」の中核を**精度改善・検証・回帰固定**した一連の作業をまとめたもの。
> NotebookLM 等への取り込み用。各タスクは「背景／対応／検証／結果」で記述。

---

## サマリ

大会ライフサイクルの3つの核心（**組合せ表の取込**・**抽選ドロー**・**当日進行**）について、
実データで精度・品質を検証し、見つかった不具合を修正し、検証結果を回帰テストとして固定した。

- テスト数: **275 → 290 件**（pass 100% / fail 0）
- 全コミットが GitHub Actions のテストゲートを通過し Oracle Cloud へ自動デプロイ済み
- ユーザの実運用ファイル `01_2026ヤサカ一般.xlsx`（5種目193名）は全工程で**漏れ・重複・誤判定ゼロ**

| コミット | 種別 | 概要 |
|---|---|---|
| `0615d11` | fix | 表未生成の `bracket/export` を404→200(error文言)に（コンソール404汚れ解消） |
| `e7de881` | fix | ダブルス横並び(所属カッコ無し)の誤読＋結合配置セルの過大計上を解消 |
| `da86ff5` | feat | seed-list取込の視覚的ファクトチェック プレビュー復活＋品質警告の提示 |
| `e765420` | test | 取込パイプライン End-to-End 回帰（parse→import→ブラケット） |
| `2a0d7a4` | test | 抽選の分散品質を実規模＋理論最小で固定 |
| `6a7dad9` | test | 当日進行の核心（勝者進出）を回帰固定 |

---

## 1. `bracket/export` の404ノイズ解消 — `0615d11`

**背景**: 「表が未生成の種目」を選ぶと `GET /bracket/export` が404を返し、正常運用なのに
ブラウザのコンソールが404で汚れていた（バグではなく想定挙動だがノイズ）。

**対応**（`server.js`、admin/public 両エンドポイント）:
- 大会が存在し**表だけ未生成** → **200** で `{error, no_bracket:true}` を返す（呼出元は従来どおり「未作成」表示）
- 大会自体が存在しない → **404** のまま（本物の not-found）

**検証**: dev server + Playwright で女子シングルス(表なし)→200、男子シングルス(表あり)→200、
存在しない大会→404、コンソールエラー0件を確認。

---

## 2. 実ファイルでの取込通し検証

**背景**: パーサ精度の総点検。`（修正）01_2026ヤサカ一般.xlsx`（実運用ファイル）を題材に。

**結果（実測100%）**:
- パーサ抽出: 5種目（混合D56・男子D30・男子S59・女子S31・女子D17）すべて
  **氏名・相方名・所属が正確**。女子ダブルスの「1ペア＝2行」レイアウトや所属の引き継ぎ（速水/藤田/金）も正解
- 取込→ブラケット生成: 全種目成功、**取込数＝配置数が完全一致・重複0・欠落0**
- as_drawn 配置: 男子シングルスは紙の連番どおり（枠1=甲vs乙…）R1配置
- 男子シングルスの紙レイアウト: 左列1〜29・右列30〜59 = 上から連番（両山印刷）と判明

---

## 3. ダブルス取込の精度改善（横並び誤読＋結合セル過大計上） — `e7de881`

**背景**: 全15サンプルでの取込回帰を実施し、ダブルスで2系統の誤読を発見。
特に `なごやか亭2021 男子ダブルス` が **196件（実数の2倍）** に膨れ、相方が半分欠落していた。

**原因と対応**（`tools/parse_bracket_seedlist.js`）:
1. **横並び誤読**: `seed|名1|名2|所属1|所属2`（所属カッコ無し）が縦ペア判定に誤落ちし、
   相方を所属フィールドに取違え＋連続2行を1ペアに誤結合していた。
   → 「**直下のseed列に番号があるか**」で横/縦を確実に分離
2. **結合配置セルの過大計上**: ブラケット配置セルが `大野・馬場（団体）` のように2姓を1セルに結合表記し、
   別載の分割シードリストと併存して二重計上。
   → 分割ペアが在るとき、結合単独セル（相方なし or 相方欄が所属断片）を冗長として除去。
     分割が皆無なら結合側を正本として温存（安全弁）

**効果（相方欠落=0達成）**:

| ファイル | 修正前 | 修正後 |
|---|---|---|
| なごやか亭2021 男子D | 196（欠落98・誤読） | **98（欠落0・全分割正確）** |
| ヤサカ杯 混合D / 男子D | 33 / 48（欠落12,20） | **17 / 24（欠落0）** |
| (修正)01ヤサカ一般（実運用） | 完璧 | **不変** |

**品質ゲート**: 既存13サンプルは**行レベルで完全同一＝無回帰**（バイト一致で確認）。
回帰テスト `test/seedlist-doubles-layout.test.js` 追加（横/縦/カッコ付き/結合除去/結合のみ温存）。

**既知の残課題（範囲外）**: `ニッタク杯_記録用.xlsx` の重複は「記録用（試合記録）」エクスポート由来の別物。
非_記録用版はクリーン（n=51）なので運用上はそちらを使用すれば回避可能。

---

## 4. 取込の視覚的ファクトチェック プレビュー復活＋品質警告 — `da86ff5`

**背景（発見したバグ）**: 取込の主経路（seed-list パーサ）は複数種目を `preview.events` で返すのに、
既存の `showImportPreview` は `preview.matches/players` しか見ておらず、
**operator が最も重視する「視覚的ファクトチェック」プレビューが空表示で機能していなかった**。
さらに直接取込時は品質警告が一切提示されず、不正ファイルでも黙って取り込まれていた。

**対応**（`public/admin/index.html` ＋ `server.js`）:
- **`showSeedListPreview`（新設）**: 取込前に種目ごとのカードで出場者一覧（#・氏名・所属・パートナー）＋
  品質警告バナー（🔴相方欠落／🔴氏名重複／ℹ組番号欠番 を色分け）＋相方なし行を赤強調。確認後に取込
- **`showImportQualityNotices`（新設）**: プレビューを省いた直接取込でも、取込後に警告を提示（黙って取り込まない安全網）
- **サーバ**: dry-run preview と実取込レスポンスの双方に `notices` を含める

**検証**: dev server + Playwright で描画（両種目・相方欠落・欠番・(相方なし)・取込ボタン）と
取込E2E（プレビュー→取込→ブラケット生成、桐山/難波 vs 飯島/元井）を確認。

---

## 5. 取込パイプライン End-to-End 回帰テスト — `e765420`

**背景**: 取込精度は手動検証してきたが、実ファイルはPIIで commit できず、
`parser→DB→ブラケット生成`の通し自動テストが無かった（回帰しても気づけない）。

**対応**（`test/import-pipeline-e2e.test.js` 新設）: PII-free な合成ワークブックを
`parseSeedList → db.importBracket(as_drawn) → ブラケット` まで通す。
- シングルス: 紙順（1,2,3..）どおりのR1配置・件数一致・重複/欠落なし
- ダブルス（横並び・所属カッコ無し）: 相方/所属の整合（別所属ペアが混ざらない）

---

## 6. 抽選ドローの検証＋回帰固定 — `2a0d7a4`

**背景**: 抽選ドロー（`drawSingleBracket`/`computeDrawLeaves`）を実規模で検証。

**検証結果（実データ規模・優秀）**:
- ヤサカ男子シングルス（59名/64枠、最大所属 釧路公立大8名）→ **R1同所属衝突 平均0・最悪0**（20試行）、深い回戦も0
- ダブルス（所属=team+partner_team の集合）も **R1=0**、深い回戦のみ許容（設計どおり）
- **分離不能ケース**（1所属が枠の過半・BIG20/32枠）→ **理論最小値4件まで最適化・全員配置・非クラッシュ**
- `drawSingleBracket` は分散品質 `r1_same_club` を operator にも返す（分離不能を可視化済み）
- → **アルゴリズムは実証的に高品質でコード変更不要**

**回帰固定**（`test/bracket-draw.test.js` に3件追加。既存は size≤32・所属≤6名までで実規模未カバーだった）:
1. 実規模・分離可能（64枠・最大所属8名・59名）→ R1同所属0件（80 seed）
2. 分離不能→理論最小 `max(0, B − size/2)` ちょうど（3ケース×40 seed、証明可能な最適性を断定）
3. ダブルス所属集合で分離可能ならR1=0（実規模）

---

## 7. 当日進行（勝者進出）の検証＋回帰固定 — `6a7dad9`

**背景**: 進行の核心（`finishMatchInternal`/`advanceWinnerInline`/`correctResult`）は
冪等性・競合検知・Elo整合性・原子性まで作り込まれた成熟コードだが、
「**勝者の次戦進出そのもの**」に専用テストが無く（elo-integrity/team-tie はElo・団体内訳のみ）、
壊れるとブラケットが静かに不整合化するリスクがあった。

**対応**（`test/progression-advance.test.js` 新設、5件。全て一発合格＝ロジック健全）:
- finish→勝者が `next_match` の正しいスロットへ進出
- BYE は自動繰り上げ（generateBracket 時に実選手が次戦へ）
- 冪等: 同じ勝者の再finishで二重進出しない（連打/オフライン再送）
- 競合: 完了済みを別勝者で finish → 上書きせず `conflict`
- 修正: `correctResult` で勝者反転 → 次戦が新勝者へ貼り替わる

---

## 技術メモ（再利用のための要点）

- **取込パーサの横/縦判定**: ダブルスのレイアウト識別は「直下のseed列に番号があるか」が最も頑健な信号。
  番号あり=1行1ペア(横)、なし=2行1ペア(縦)。所属カッコの有無に依存しない。
- **結合セル除去の安全弁**: 「分割ペアが1件でも在るときだけ結合単独セルを落とす」。
  結合表記しか無いファイルは正本として温存し、データ消失を防ぐ。
- **as_drawn は全選手に組番号(seed≥1)が必要**。パーサは常に 1..N を付与するため実フローでは問題なし。
- **抽選の分離品質**: 分離可能ならR1同所属0、分離不能でも `max(0, B−size/2)` の理論最小。
  most-constrained-first 配置 ＋ R1同所属の swap 修復で達成。再現性は draw_seed と id 全順序化で担保。
- **進行の安全性**: finish は冪等ガード（同一勝者は無害）＋競合検知（別勝者は上書きせず conflict）。
  結果変更は必ず `correctResult` 経由（次戦クリア→再進出を1トランザクション）。
- **PII対策**: 実大会ファイルは commit 不可。回帰は合成データ（PII-free）でパイプライン全体を固定する方針。

---

# 続編 — viewer 刷新・データ精度・全機能の検証/堅牢化（同日後半）

> 上記（取込・抽選・進行の核心）に続き、viewer（公開閲覧/観戦）刷新、データ精度の追加修正、
> 残る全機能（団体・監督・進行UI・観戦live）の検証と回帰固定、新機能（待機理由表示）まで実施。
> テスト数 **290 → 308 件**（pass 100%）。全コミット デプロイ済み。

| コミット | 種別 | 概要 |
|---|---|---|
| `f3af39c` | fix | viewer: 未対戦の将来戦を「不戦勝」と誤表示→「vs(予定)」に |
| `50808f0` | style/fix | viewer 大会一覧/詳細/結果を丹頂化＋**--accent 伝播バグ修正**（全体が #dc2626 のままだったのを #c01526 に） |
| `6868644` | fix | 観戦コート盤面を court_count に合わせる（小規模大会で空き44台が並ぶ不具合） |
| `55e3fe5` | style | 結果ビューの「? vs ?（対戦者未確定）」将来戦を件数に畳んでノイズ削減 |
| `e16928b` | fix | 混合先行で女子選手が male になる性別誤りを訂正（性別明記種目を正本に） |
| `c0f330d` | style | 選手プロフィール/H2H/ランキング/進行を丹頂トンマナに統一（総仕上げ） |
| `f38b174` | fix | **団体戦で登録団体チーム（釧友会/サザンクロス等）が消える不具合**を修正 |
| `840bde3` | fix | 罫線表の複数行所属カッコの断片を氏名と誤読しない（Nittaku杯 相方欠落解消） |
| `01b038c` | test | 種目優先順位ロック（中核）の回帰を追加 |
| `ee26cba` | test | 監督モードの認証・チーム間分離の回帰（#285〜/#292） |
| `edd8721` | style/fix | 観戦ライブ仕上げ（完了表示・スクロール保持・可読性・丹頂統一・Esc） |
| `521c753` | feat | 観戦liveの残試合に**待機理由（呼べない理由）を表示** |

## 8. viewer 丹頂エディトリアル刷新（`f3af39c`/`50808f0`/`55e3fe5`/`c0f330d`）
**背景**: 公開 viewer のデザイン刷新「丹頂エディトリアル（白基調・丹頂レッド #c01526・ゴシック・カード）」の総仕上げ。
**対応**:
- `f3af39c`: 大会結果ビューは BYE/不戦勝を除外済みのため、`renderMatchResultRow` に来るのは「完了」か「未対戦」。旧実装は `isBye=!loser_name` で**未対戦の将来戦を一律「不戦勝」と誤表示**（取込直後は全将来戦が不戦勝）。完了済みのみ結果/不戦勝とし、未対戦は「選手 vs 選手(予定)」に。
- `50808f0`: 大会一覧/詳細/結果を丹頂化。**重要な伝播バグ**: common.css が `--accent: var(--kt-accent)` を `:root` で定義するため、viewer body の `--kt-accent` 上書きが `--accent` に伝播せず、viewer 全体が本来の丹頂 #c01526 でなく #dc2626 のままだった。body で `--accent/-l/-bg` を直接上書きして解消。
- `55e3fe5`: 結果ビューの「? vs ?（両者未確定）」将来戦を「他 N 試合 — 対戦者未確定」に畳み、骨組みノイズを削減（実データで 191→1 行）。
- `c0f330d`: 選手プロフィール戦績(pm-row)・H2H・ランキング・進行ライブコートの残ハードコード色（紺 --pri/旧赤#dc2626/汎用緑赤/灰）を丹頂トークンへ統一。新トークン `--lg-win-bg`。
**結果**: viewer 本体の明色ムラがほぼ解消。computed style で各色が丹頂パレットに解決することを確認。

## 9. 観戦コート数（`6868644`）
**背景/対応**: 観戦コート盤面が既定レイアウト `court_rows×court_cols=4×11=44` を court_count に関わらず使い、4コート運用でも44台（空き43）を並べていた。既定レイアウトのまま かつ court_count≠44 のときだけ court_count から実コート数のグリッドを導出（明示レイアウトは尊重）。`getOperationState` の tables/tournament 双方に反映。
**結果**: court_count=4 で tables 44→4。回帰 `test/live-court-grid.js`（4/8/12/44台・明示尊重・呼出セル保持）。

## 10. 選手の性別誤り（`e16928b`）
**背景**: 混合ダブルスは性別不定で選手が既定 male 作成。複数種目の処理順で混合が女子選手を先に male で作ると、後続の女子種目で更新されず「女子なのに male」が残存（実データで難波/市橋/村中/松本 等）。
**対応**: `importFromSeedList` で性別明記種目（女子/男子＝`_eventGender`）に連携/作成する際、既存選手の性別を正本として訂正（混合では訂正しない＝不定を温存）。
**結果**: 再取込で女子シングルス31人全員 female、桐山(混合+男子D)は male 維持。回帰 `test/import-gender-fix.js`。既存取込済み大会は再取込で訂正。

## 11. 団体戦で登録団体チームが消える（`f38b174`）★重大
**背景（TDDで発見）**: 取込E2Eに団体ケースを追加中、登録団体名のチーム（釧友会・サザンクロス・AMATAKU 等）が**ブラケットから消える**のを発見。登録団体ガード `guardRegisteredTeams`（団体名を選手名スロットから弾く）が、団体戦では「チーム名＝entrant名」なのに名前を剥がしていた。団体戦に出るのはまさに登録クラブ/学校なので影響大。
**対応**: `importFromSeedList` で種目名から団体戦を判定（parser guessFormat と同基準）し、団体戦ではガードと選手マスタ連携/作成をスキップ（チームは選手化しない）。
**結果**: 合成E2E（釧友会=4チーム目温存）、実ファイル ver3.チームカップ男子で46チーム全残・サザンクロス残存・空名0。

## 12. パーサ: 複数行所属カッコ断片の誤読（`840bde3`）
**背景**: Nittaku杯/なごやか亭の罫線表は所属カッコが複数行に折返し、閉じ括弧つき断片（「Neo倶楽部）」「（釧路市役所・」等）がセルに残り氏名として誤読→混合Dで相方欠落の幻影ペア。
**対応**: `looksLikeName` でカッコ（（）()）を含む文字列を氏名から除外。クリーンなシードリスト由来の本物ペアは保持。
**結果**: Nittaku杯 混合D 45/50→38(相方空0)。既存良好ファイルは行レベル不変・無回帰。

## 13. 全機能の検証＋回帰固定（`01b038c`/`ee26cba` ＋ 検証のみ）
- **種目優先順位ロック**（中核・`01b038c`）: 団体>混合D>ダブルス>シングルス。上位種目で生存中の選手は下位で呼べない。force で強制可・上位決着後は解除・同下位は非ロック。回帰 `test/priority-lock.js`。
- **監督モード**（`ee26cba`）: コード認証・チーム間分離（自チームのロスター/修正申請のみ）・共同監督メンバーコード(#292)・親無効化連鎖・登録上限。回帰 `test/coach-mode.js`。
- **帳票**（検証のみ・無修正）: 両山トーナメント表/集計表/対戦票/領収書が実データで正しく生成（既存テスト網羅済み）。
- **進行管理UI**（admin・Playwright通し検証・無修正）: コート盤面→呼出フロー→優先度ロック→結果入力→進出→修正→取消が正しく動作、コンソール0。
- **実大会通しリハーサル**（無修正）: 取込→抽選→進行→修正→undo→viewer の全連携が正常。

## 14. 観戦ライブの仕上げ（`edd8721`）
**背景/対応**: 観戦live（public/viewer/live）を磨き込み。①進捗100%で「🏁 全試合終了」明示 ②ライブ更新の全再描画で失われていたコート盤面の横/ページ縦スクロール位置を保持 ③小型スマホ(≤380px)の選手名 9px→11px ④部分ハードコード色を `--lv-*` トークンへ統一 ⑤詳細/QRモーダルを Esc で閉じる。
**結果**: Playwright/computed で全項目確認・コンソール0。backend不変。

## 15. 待機理由の可視化（新機能・`521c753`）
**背景**: 観戦者/監督が「なぜこの試合がまだ呼ばれないか」を把握できるよう、待機理由（上位種目で生存中・試合中・審判担当中）を表示。
**対応**: 公開 live API（`slimPublicState`）は既に `callable[].blocks` を射影済み。観戦に不要な内部ID `locked_by_match` のみ除去（PII/内部メタ最小化）。観戦live の「残試合」に「⏳ 待機: {label}」を丹頂アンバー（--lv-warn）で表示（選手名は出さず種目＋理由のみ）。メインviewerは既に実装済みのため非改変。
**結果**: /live が65/95件にblocks付与（locked_by_match無）、live で「⏳待機: 混合ダブルス で勝ち上がり中」を丹頂色表示。回帰 server-smoke (g2)。

## 追加の技術メモ
- **CSS変数の伝播**: `--accent: var(--kt-accent)` を `:root` で定義すると、子要素(body)での `--kt-accent` 上書きは `--accent` に伝播しない（変数は定義箇所の値で確定）。アクセントを差し替えるなら `--accent` を直接上書きする。
- **登録団体ガード × 団体戦**: ガードは「団体名を選手名から弾く」用。団体戦は「チーム名＝entrant名」なので**ガードを通してはいけない**（種目名で団体判定してスキップ）。回帰テストで保護。
- **性別の正本**: 混合ダブルスでは性別が決まらない。性別明記種目（女子/男子）が正本で、既存選手の性別を訂正してよい。
- **公開API射影**: viewer/live は内部FK・トークン・rating差分・locked_by_match 等を落として射影（PII/内部メタ最小化）。表示に要る列のみ素通し。
